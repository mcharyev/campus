{% extends 'main.html.twig' %}

{% block moduletitle %}Discipline Action Form{% endblock %}
{% block main %}
    <div style="width:90%;margin:auto;"><h3>Discipline Action Form</h3></div>
    <div style="width:90%;margin:auto;">
        {% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Create / Update</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
{% block customjs %}
    <script>
        jQuery(document).ready(function () {
            //populate_items($('#taught_course_form_student_groups').val());
            //autocompleting();
        });
    </script>
{% endblock %}{# empty Twig template #}
